;;; ( SIMPLY SCHEME EXERCISE 14.3 ) Write the recursive remove duplicates procedure.

(define (remdup sent)
  (cond ((empty? sent) '())
        ((member? (first sent) (bf sent)) (se (remdup (bf sent))))
        (else (se (first sent) (remdup (bf sent))))))
