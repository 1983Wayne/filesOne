;;; ( SIMPLY SCHEME EXERCISE 11.5 ) Write a procedure that returns all the first letters of a sentence argument.

(define (initials sent)
  (if (equal? (count sent) 0)
      '()
      (se (first (first sent)) (initials (bf sent)))))
