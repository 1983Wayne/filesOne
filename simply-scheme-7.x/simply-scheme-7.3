;;; ( SIMPLY SCHEME EXERCISE 7.3 ) Fix the issue with the following program :
;;;
;;; (define (superlative adjective word)
;;;    (se (word adjective 'est) word))
;;;
;;; So that (superlative 'dumb 'exercise) will return (dumbest exercise)

(define (superlative adjective wd)
  (se (word adjective 'est) wd))
